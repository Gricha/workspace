---
sidebar_position: 1
---

# Configuration

Config file: `~/.workspace-agent/config.yaml`

```yaml
credentials:
  env:
    KEY: "value"
  files:
    ~/.ssh/id_ed25519: ~/.ssh/id_ed25519
```

Edit via Web UI → Settings or directly in file.

Restart workspaces to apply changes.
