---
sidebar_position: 2
---

# Environment Variables

Add to `config.yaml`:

```yaml
credentials:
  env:
    DATABASE_URL: "postgres://..."
    API_KEY: "..."
```

Or Web UI → Settings → Environment.

Available in all workspaces.
