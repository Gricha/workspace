---
sidebar_position: 3
---

# Files

Copy files into workspaces:

```yaml
credentials:
  files:
    ~/.ssh/id_ed25519: ~/.ssh/id_ed25519
    ~/.gitconfig: ~/.gitconfig
    ~/.npmrc: ~/.npmrc
```

Or Web UI → Settings → Files.

Format: `destination: source`
